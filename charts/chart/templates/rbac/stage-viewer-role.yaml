{{- if eq .Values.mode "operator" }}
{{- if .Values.rbac.helpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "paprika.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "paprika.resourceName" (dict "suffix" "stage-viewer-role" "context" $) }}
rules:
- apiGroups:
  - pipelines.paprika.io
  resources:
  - stages
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - pipelines.paprika.io
  resources:
  - stages/status
  verbs:
  - get
{{- end }}
{{- end }}
