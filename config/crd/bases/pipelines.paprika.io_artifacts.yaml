---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: artifacts.pipelines.paprika.io
spec:
  group: pipelines.paprika.io
  names:
    kind: Artifact
    listKind: ArtifactList
    plural: artifacts
    singular: artifact
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Artifact represents a build artifact.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ArtifactSpec defines the specification for an artifact.
            properties:
              digest:
                type: string
              provenance:
                description: |-
                  ArtifactProvenance tracks the origin of an artifact.
                  ArtifactProvenance tracks the origin of an artifact.
                properties:
                  build:
                    type: string
                  pipeline:
                    type: string
                type: object
              reference:
                type: string
              type:
                enum:
                - oci
                type: string
            required:
            - reference
            - type
            type: object
          status:
            description: ArtifactStatus represents the status of an artifact.
            properties:
              verified:
                type: boolean
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
