---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: applications.pipelines.paprika.io
spec:
  group: pipelines.paprika.io
  names:
    kind: Application
    listKind: ApplicationList
    plural: applications
    singular: application
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.currentStage
      name: Stage
      type: string
    - jsonPath: .status.revision
      name: Revision
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Application represents a Paprika application.
          Application represents a Paprika application.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ApplicationSpec defines the specification for an application.
              ApplicationSpec defines the specification for an application.
            properties:
              analysisTemplates:
                description: |-
                  AnalysisTemplates references reusable analysis templates that run continuously
                  in the background after the application is healthy.
                items:
                  description: AnalysisTemplateRef references an AnalysisTemplate
                    and supplies arguments.
                  properties:
                    args:
                      additionalProperties:
                        type: string
                      description: Args override template arguments.
                      type: object
                    intervalSeconds:
                      description: IntervalSeconds overrides the default analysis
                        interval.
                      type: integer
                    name:
                      description: Name of the AnalysisTemplate to use.
                      type: string
                    onFailure:
                      description: OnFailure defines the action to take when the analysis
                        fails.
                      properties:
                        action:
                          enum:
                          - rollback
                          - halt
                          - ignore
                          type: string
                        notify:
                          items:
                            type: string
                          type: array
                      required:
                      - action
                      type: object
                  required:
                  - name
                  type: object
                type: array
              approvalGates:
                description: ApprovalGates define manual approval gates for stage
                  transitions.
                items:
                  description: ApprovalGate defines a manual or automated approval
                    gate for stage transitions.
                  properties:
                    body:
                      description: Body template sent to webhook gates.
                      type: string
                    headers:
                      additionalProperties:
                        type: string
                      description: Headers to send with webhook requests.
                      type: object
                    method:
                      default: POST
                      description: HTTP method for webhook gates. Defaults to POST.
                      type: string
                    name:
                      description: Name of the gate
                      type: string
                    required:
                      default: true
                      description: Whether the gate is required (default true)
                      type: boolean
                    secretRef:
                      description: SecretRef names a Secret in the same namespace
                        whose data is added to webhook headers.
                      type: string
                    slackChannel:
                      description: SlackChannel is the channel to notify for Slack
                        gates.
                      type: string
                    slackWebhookUrl:
                      description: |-
                        SlackWebhookURL is the incoming Slack webhook URL used to notify a channel.
                        Phase 2: actual Slack interaction handling.
                      type: string
                    stage:
                      description: Stage at which this gate applies (e.g., "prod").
                        Empty applies to all stages.
                      type: string
                    successStatus:
                      default: 200
                      description: SuccessStatus is the expected HTTP status code
                        for approval (default any 2xx).
                      type: integer
                    type:
                      description: 'Type of gate: manual, webhook, slack'
                      enum:
                      - manual
                      - webhook
                      - slack
                      type: string
                    url:
                      description: URL is the webhook URL for webhook gates.
                      type: string
                  required:
                  - name
                  - type
                  type: object
                type: array
              build:
                description: Build defines the CI pipeline steps (optional — skip
                  if no build needed).
                properties:
                  artifacts:
                    items:
                      description: PipelineOutput defines an output artifact of a
                        pipeline.
                      properties:
                        name:
                          type: string
                        path:
                          type: string
                        step:
                          type: string
                      required:
                      - name
                      - path
                      type: object
                    type: array
                  maxParallel:
                    type: integer
                  sources:
                    items:
                      description: Source defines a source for the pipeline.
                      properties:
                        secretRef:
                          type: string
                        type:
                          enum:
                          - git
                          type: string
                        url:
                          type: string
                      required:
                      - type
                      type: object
                    type: array
                  steps:
                    items:
                      description: |-
                        ApplicationBuildStep defines a step in the build pipeline.
                        ApplicationBuildStep defines a step in the build pipeline.
                      properties:
                        depends:
                          items:
                            type: string
                          type: array
                        image:
                          type: string
                        name:
                          type: string
                        retry:
                          type: integer
                        script:
                          type: string
                        timeout:
                          type: integer
                      required:
                      - image
                      - name
                      - script
                      type: object
                    type: array
                required:
                - steps
                type: object
              canary:
                description: |-
                  Canary defines the default canary configuration.
                  Overridden by per-stage canary config.
                properties:
                  analysis:
                    description: PDV analysis configuration
                    properties:
                      checks:
                        items:
                          description: |-
                            AnalysisCheck defines a check for canary analysis.
                            AnalysisCheck defines a check for canary analysis.
                          properties:
                            httpHeaders:
                              additionalProperties:
                                type: string
                              description: HTTP headers to send with the request
                              type: object
                            metric:
                              description: 'Pod metric to check (for type=podMetrics):
                                errorRate, latencyP99, restartRate'
                              enum:
                              - errorRate
                              - latencyP99
                              - restartRate
                              type: string
                            name:
                              description: Name of the check.
                              type: string
                            requestCount:
                              description: Number of requests to make per analysis
                                cycle
                              type: integer
                            successThreshold:
                              description: Fraction of requests that must succeed
                                as a percentage string (e.g. "99" = 99%)
                              type: string
                            threshold:
                              description: 'Threshold as a string (errorRate: "0.01"
                                = 1%, latencyP99: "500" = 500ms, restartRate: "3"
                                = count)'
                              type: string
                            timeoutSeconds:
                              description: Timeout per request in seconds
                              type: integer
                            type:
                              enum:
                              - http
                              - podMetrics
                              type: string
                            url:
                              description: URL to probe (for type=http)
                              type: string
                            windowSeconds:
                              description: Time window in seconds to evaluate the
                                metric
                              type: integer
                          required:
                          - type
                          type: object
                        type: array
                      intervalSeconds:
                        type: integer
                      rollbackOnFail:
                        type: boolean
                    type: object
                  intervalSeconds:
                    description: Seconds to wait between canary weight steps
                    type: integer
                  steps:
                    description: Traffic weight percentages at each canary step, e.g.
                      [10, 30, 60, 100]
                    items:
                      type: integer
                    type: array
                required:
                - steps
                type: object
              conftestPolicies:
                description: |-
                  ConftestPolicies are Rego policies evaluated against rendered manifests before each
                  promotion. References are namespace-scoped. A missing or uncompilable policy blocks
                  promotion (fail-closed).
                items:
                  description: ConftestPolicyRef references a ConftestPolicy by name
                    in the Application's namespace.
                  properties:
                    name:
                      description: Name of the ConftestPolicy.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              healthChecks:
                description: HealthChecks define custom CEL-based health checks for
                  the application.
                items:
                  description: |-
                    HealthCheck defines a health check.
                    HealthCheck defines a health check.
                  properties:
                    expression:
                      description: |-
                        CEL expression to evaluate. Available variables:
                          app (Application spec), status (Application status), http (HTTP probe results).
                        Expression must return a boolean or a string matching HealthStatus.
                      type: string
                    httpProbe:
                      description: |-
                        Optional HTTP probe to run before evaluating the expression.
                        Results available in CEL as http.status, http.body, http.headers.
                      properties:
                        body:
                          description: Request body (for POST/PUT)
                          type: string
                        expectedStatus:
                          description: Expected HTTP status code (default 200)
                          type: integer
                        headers:
                          additionalProperties:
                            type: string
                          description: Request headers
                          type: object
                        method:
                          default: GET
                          description: HTTP method (GET, POST, PUT, DELETE, HEAD)
                          type: string
                        timeout:
                          default: 5
                          description: Timeout in seconds (default 5)
                          type: integer
                        url:
                          description: URL to probe
                          type: string
                      required:
                      - url
                      type: object
                    interval:
                      default: 30s
                      description: How often to run this check (default 30s)
                      type: string
                    name:
                      description: Name of the health check
                      type: string
                  required:
                  - expression
                  - name
                  type: object
                type: array
              onFailure:
                description: OnFailure defines the action when a promotion fails.
                properties:
                  action:
                    enum:
                    - rollback
                    - halt
                    - ignore
                    type: string
                  notify:
                    items:
                      type: string
                    type: array
                required:
                - action
                type: object
              parameters:
                additionalProperties:
                  type: string
                description: Parameters are Helm value overrides passed to all releases.
                type: object
              project:
                default: default
                description: Project references the AppProject that governs this application.
                type: string
              selfHeal:
                description: SelfHeal controls automatic remediation when drift or
                  health failures are detected.
                properties:
                  autoRevertOnHealthFailure:
                    description: AutoRevertOnHealthFailure rolls back the current
                      release when the application becomes Degraded.
                    type: boolean
                  autoSyncOnDrift:
                    description: AutoSyncOnDrift triggers a re-sync when managed resources
                      are out of sync.
                    type: boolean
                  cooldown:
                    default: 5m
                    description: Cooldown between self-heal actions. Defaults to 5m.
                    type: string
                type: object
              source:
                description: Source defines where the application code/chart lives.
                properties:
                  bucket:
                    description: S3 bucket (for type=s3)
                    type: string
                  chart:
                    description: Helm chart reference (for type=helm)
                    properties:
                      name:
                        type: string
                      path:
                        description: Path to a local Helm chart directory (alternative
                          to Repo+Name)
                        type: string
                      repo:
                        type: string
                      version:
                        type: string
                    type: object
                  endpoint:
                    description: S3 endpoint URL (for type=s3, use LocalStack endpoint
                      for testing)
                    type: string
                  image:
                    description: 'Deprecated: Image is the legacy OCI URL field. Use
                      oci.url instead.'
                    type: string
                  inline:
                    description: Inline references a manifest snapshot ConfigMap (for
                      type=inline).
                    properties:
                      configMapRef:
                        description: ConfigMapRef is the name of the ConfigMap containing
                          the rendered manifest bundle.
                        type: string
                    type: object
                  insecure:
                    description: Insecure allows plain HTTP for OCI registries (type=oci)
                    type: boolean
                  key:
                    description: S3 object key (for type=s3)
                    type: string
                  oci:
                    description: OCI registry reference (for type=oci)
                    properties:
                      insecure:
                        description: Insecure allows plain HTTP for the OCI registry
                        type: boolean
                      secretRef:
                        description: SecretRef references a Secret with dockerconfigjson
                          or .dockerconfigjson
                        type: string
                      tag:
                        description: Tag or digest of the artifact (e.g. "1.2.3",
                          "@sha256:...")
                        type: string
                      url:
                        description: URL of the OCI artifact, e.g. oci://registry.example.com/charts/mychart
                        pattern: ^oci://
                        type: string
                    required:
                    - url
                    type: object
                  path:
                    description: Path within the repo to the chart/source (for type=git
                      or type=s3)
                    type: string
                  pollInterval:
                    default: 30s
                    description: Poll interval for change detection (default 30s)
                    type: string
                  region:
                    description: S3 region (for type=s3)
                    type: string
                  repoRef:
                    description: |-
                      RepoRef references a core.paprika.io Repository by name. When set, takes
                      precedence over inline URL/credentials fields.
                    type: string
                  repoUrl:
                    description: Git repository URL (for type=git)
                    type: string
                  revision:
                    description: Git branch, tag, or commit (for type=git)
                    type: string
                  secretRef:
                    description: |-
                      Secret reference for private repos, S3, or OCI credentials.
                      For OCI this should contain dockerconfigjson or username/password.
                    type: string
                  type:
                    enum:
                    - git
                    - helm
                    - kustomize
                    - s3
                    - oci
                    - inline
                    type: string
                required:
                - type
                type: object
              stages:
                description: Stages defines the promotion environments (dev, staging,
                  prod, etc.).
                items:
                  description: |-
                    ApplicationPromotionStage defines a promotion stage.
                    ApplicationPromotionStage defines a promotion stage.
                  properties:
                    approvalGates:
                      description: ApprovalGates define manual/webhook/Slack approval
                        gates for promotion into this stage.
                      items:
                        description: ApprovalGate defines a manual or automated approval
                          gate for stage transitions.
                        properties:
                          body:
                            description: Body template sent to webhook gates.
                            type: string
                          headers:
                            additionalProperties:
                              type: string
                            description: Headers to send with webhook requests.
                            type: object
                          method:
                            default: POST
                            description: HTTP method for webhook gates. Defaults to
                              POST.
                            type: string
                          name:
                            description: Name of the gate
                            type: string
                          required:
                            default: true
                            description: Whether the gate is required (default true)
                            type: boolean
                          secretRef:
                            description: SecretRef names a Secret in the same namespace
                              whose data is added to webhook headers.
                            type: string
                          slackChannel:
                            description: SlackChannel is the channel to notify for
                              Slack gates.
                            type: string
                          slackWebhookUrl:
                            description: |-
                              SlackWebhookURL is the incoming Slack webhook URL used to notify a channel.
                              Phase 2: actual Slack interaction handling.
                            type: string
                          stage:
                            description: Stage at which this gate applies (e.g., "prod").
                              Empty applies to all stages.
                            type: string
                          successStatus:
                            default: 200
                            description: SuccessStatus is the expected HTTP status
                              code for approval (default any 2xx).
                            type: integer
                          type:
                            description: 'Type of gate: manual, webhook, slack'
                            enum:
                            - manual
                            - webhook
                            - slack
                            type: string
                          url:
                            description: URL is the webhook URL for webhook gates.
                            type: string
                        required:
                        - name
                        - type
                        type: object
                      type: array
                    autoPromote:
                      description: Auto-promote to the next ring after this stage
                        is healthy
                      type: boolean
                    canary:
                      description: Canary config for this stage (overrides spec.canary
                        if set)
                      properties:
                        analysis:
                          description: PDV analysis configuration
                          properties:
                            checks:
                              items:
                                description: |-
                                  AnalysisCheck defines a check for canary analysis.
                                  AnalysisCheck defines a check for canary analysis.
                                properties:
                                  httpHeaders:
                                    additionalProperties:
                                      type: string
                                    description: HTTP headers to send with the request
                                    type: object
                                  metric:
                                    description: 'Pod metric to check (for type=podMetrics):
                                      errorRate, latencyP99, restartRate'
                                    enum:
                                    - errorRate
                                    - latencyP99
                                    - restartRate
                                    type: string
                                  name:
                                    description: Name of the check.
                                    type: string
                                  requestCount:
                                    description: Number of requests to make per analysis
                                      cycle
                                    type: integer
                                  successThreshold:
                                    description: Fraction of requests that must succeed
                                      as a percentage string (e.g. "99" = 99%)
                                    type: string
                                  threshold:
                                    description: 'Threshold as a string (errorRate:
                                      "0.01" = 1%, latencyP99: "500" = 500ms, restartRate:
                                      "3" = count)'
                                    type: string
                                  timeoutSeconds:
                                    description: Timeout per request in seconds
                                    type: integer
                                  type:
                                    enum:
                                    - http
                                    - podMetrics
                                    type: string
                                  url:
                                    description: URL to probe (for type=http)
                                    type: string
                                  windowSeconds:
                                    description: Time window in seconds to evaluate
                                      the metric
                                    type: integer
                                required:
                                - type
                                type: object
                              type: array
                            intervalSeconds:
                              type: integer
                            rollbackOnFail:
                              type: boolean
                          type: object
                        intervalSeconds:
                          description: Seconds to wait between canary weight steps
                          type: integer
                        steps:
                          description: Traffic weight percentages at each canary step,
                            e.g. [10, 30, 60, 100]
                          items:
                            type: integer
                          type: array
                      required:
                      - steps
                      type: object
                    cluster:
                      description: Cluster to deploy to (defaults to same cluster)
                      properties:
                        agentAddress:
                          type: string
                        kubeconfigSecret:
                          type: string
                        mode:
                          description: ClusterMode defines how the controller connects
                            to a cluster.
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                        server:
                          type: string
                        serviceAccount:
                          type: string
                      required:
                      - name
                      type: object
                    gates:
                      description: Verification gates to run after promotion
                      items:
                        description: |-
                          GateConfig defines a gate for stage promotion.
                          GateConfig defines a gate for stage promotion.
                        properties:
                          endpoint:
                            type: string
                          timeout:
                            type: integer
                          type:
                            enum:
                            - smoke-test
                            - duration
                            type: string
                        required:
                        - type
                        type: object
                      type: array
                    name:
                      type: string
                    parameters:
                      additionalProperties:
                        type: string
                      description: Feature flag / parameter overrides for this stage
                      type: object
                    ring:
                      description: Ring number (lower = earlier environment)
                      type: integer
                    rolloutStrategy:
                      description: |-
                        RolloutStrategy is an advanced deployment strategy for this stage.
                        Mutually exclusive with Canary.
                      properties:
                        abTest:
                          description: ABTestStrategy routes traffic to stable or
                            canary based on headers or cookies.
                          properties:
                            analysis:
                              description: RolloutAnalysis defines analysis checks
                                for a Rollout.
                              properties:
                                checks:
                                  items:
                                    description: AnalysisCheck defines a single analysis
                                      check.
                                    properties:
                                      httpHeaders:
                                        additionalProperties:
                                          type: string
                                        description: HTTP headers to send with the
                                          request
                                        type: object
                                      metric:
                                        description: Pod metric to check (for type=podMetrics)
                                        enum:
                                        - errorRate
                                        - latencyP99
                                        - restartRate
                                        type: string
                                      requestCount:
                                        description: Number of requests to make per
                                          analysis cycle
                                        type: integer
                                      successThreshold:
                                        description: Fraction of requests that must
                                          succeed as a percentage string
                                        type: string
                                      threshold:
                                        description: Threshold as a string
                                        type: string
                                      timeoutSeconds:
                                        description: Timeout per request in seconds
                                        type: integer
                                      type:
                                        enum:
                                        - http
                                        - podMetrics
                                        type: string
                                      url:
                                        description: URL to probe (for type=http)
                                        type: string
                                      windowSeconds:
                                        description: Time window in seconds to evaluate
                                          the metric
                                        type: integer
                                    required:
                                    - type
                                    type: object
                                  type: array
                                failedThreshold:
                                  format: int32
                                  type: integer
                                interval:
                                  type: string
                                successThreshold:
                                  format: int32
                                  type: integer
                              type: object
                            canaryService:
                              type: string
                            routes:
                              items:
                                description: ABTestRoute matches a header or cookie
                                  and routes to a service.
                                properties:
                                  name:
                                    type: string
                                  service:
                                    type: string
                                  type:
                                    type: string
                                  value:
                                    type: string
                                required:
                                - name
                                - service
                                - type
                                - value
                                type: object
                              type: array
                            stableService:
                              type: string
                          required:
                          - routes
                          type: object
                        blueGreen:
                          description: BlueGreenStrategy runs the new version behind
                            a preview service, then cuts over.
                          properties:
                            activeService:
                              type: string
                            analysis:
                              description: RolloutAnalysis defines analysis checks
                                for a Rollout.
                              properties:
                                checks:
                                  items:
                                    description: AnalysisCheck defines a single analysis
                                      check.
                                    properties:
                                      httpHeaders:
                                        additionalProperties:
                                          type: string
                                        description: HTTP headers to send with the
                                          request
                                        type: object
                                      metric:
                                        description: Pod metric to check (for type=podMetrics)
                                        enum:
                                        - errorRate
                                        - latencyP99
                                        - restartRate
                                        type: string
                                      requestCount:
                                        description: Number of requests to make per
                                          analysis cycle
                                        type: integer
                                      successThreshold:
                                        description: Fraction of requests that must
                                          succeed as a percentage string
                                        type: string
                                      threshold:
                                        description: Threshold as a string
                                        type: string
                                      timeoutSeconds:
                                        description: Timeout per request in seconds
                                        type: integer
                                      type:
                                        enum:
                                        - http
                                        - podMetrics
                                        type: string
                                      url:
                                        description: URL to probe (for type=http)
                                        type: string
                                      windowSeconds:
                                        description: Time window in seconds to evaluate
                                          the metric
                                        type: integer
                                    required:
                                    - type
                                    type: object
                                  type: array
                                failedThreshold:
                                  format: int32
                                  type: integer
                                interval:
                                  type: string
                                successThreshold:
                                  format: int32
                                  type: integer
                              type: object
                            autoPromotionSeconds:
                              format: int32
                              type: integer
                            previewReplicaCount:
                              format: int32
                              type: integer
                            previewService:
                              type: string
                            scaleDownDelaySeconds:
                              format: int32
                              type: integer
                          required:
                          - activeService
                          type: object
                        canary:
                          description: CanaryStrategy progressively shifts traffic
                            to the canary version.
                          properties:
                            analysis:
                              description: RolloutAnalysis defines analysis checks
                                for a Rollout.
                              properties:
                                checks:
                                  items:
                                    description: AnalysisCheck defines a single analysis
                                      check.
                                    properties:
                                      httpHeaders:
                                        additionalProperties:
                                          type: string
                                        description: HTTP headers to send with the
                                          request
                                        type: object
                                      metric:
                                        description: Pod metric to check (for type=podMetrics)
                                        enum:
                                        - errorRate
                                        - latencyP99
                                        - restartRate
                                        type: string
                                      requestCount:
                                        description: Number of requests to make per
                                          analysis cycle
                                        type: integer
                                      successThreshold:
                                        description: Fraction of requests that must
                                          succeed as a percentage string
                                        type: string
                                      threshold:
                                        description: Threshold as a string
                                        type: string
                                      timeoutSeconds:
                                        description: Timeout per request in seconds
                                        type: integer
                                      type:
                                        enum:
                                        - http
                                        - podMetrics
                                        type: string
                                      url:
                                        description: URL to probe (for type=http)
                                        type: string
                                      windowSeconds:
                                        description: Time window in seconds to evaluate
                                          the metric
                                        type: integer
                                    required:
                                    - type
                                    type: object
                                  type: array
                                failedThreshold:
                                  format: int32
                                  type: integer
                                interval:
                                  type: string
                                successThreshold:
                                  format: int32
                                  type: integer
                              type: object
                            canaryService:
                              type: string
                            stableService:
                              type: string
                            steps:
                              items:
                                description: CanaryStep defines a single step in a
                                  canary rollout.
                                properties:
                                  analysis:
                                    description: RolloutAnalysis defines analysis
                                      checks for a Rollout.
                                    properties:
                                      checks:
                                        items:
                                          description: AnalysisCheck defines a single
                                            analysis check.
                                          properties:
                                            httpHeaders:
                                              additionalProperties:
                                                type: string
                                              description: HTTP headers to send with
                                                the request
                                              type: object
                                            metric:
                                              description: Pod metric to check (for
                                                type=podMetrics)
                                              enum:
                                              - errorRate
                                              - latencyP99
                                              - restartRate
                                              type: string
                                            requestCount:
                                              description: Number of requests to make
                                                per analysis cycle
                                              type: integer
                                            successThreshold:
                                              description: Fraction of requests that
                                                must succeed as a percentage string
                                              type: string
                                            threshold:
                                              description: Threshold as a string
                                              type: string
                                            timeoutSeconds:
                                              description: Timeout per request in
                                                seconds
                                              type: integer
                                            type:
                                              enum:
                                              - http
                                              - podMetrics
                                              type: string
                                            url:
                                              description: URL to probe (for type=http)
                                              type: string
                                            windowSeconds:
                                              description: Time window in seconds
                                                to evaluate the metric
                                              type: integer
                                          required:
                                          - type
                                          type: object
                                        type: array
                                      failedThreshold:
                                        format: int32
                                        type: integer
                                      interval:
                                        type: string
                                      successThreshold:
                                        format: int32
                                        type: integer
                                    type: object
                                  duration:
                                    type: string
                                  setWeight:
                                    format: int32
                                    type: integer
                                required:
                                - setWeight
                                type: object
                              type: array
                          required:
                          - steps
                          type: object
                        mirror:
                          description: MirrorStrategy mirrors a percentage of traffic
                            to canary.
                          properties:
                            analysis:
                              description: RolloutAnalysis defines analysis checks
                                for a Rollout.
                              properties:
                                checks:
                                  items:
                                    description: AnalysisCheck defines a single analysis
                                      check.
                                    properties:
                                      httpHeaders:
                                        additionalProperties:
                                          type: string
                                        description: HTTP headers to send with the
                                          request
                                        type: object
                                      metric:
                                        description: Pod metric to check (for type=podMetrics)
                                        enum:
                                        - errorRate
                                        - latencyP99
                                        - restartRate
                                        type: string
                                      requestCount:
                                        description: Number of requests to make per
                                          analysis cycle
                                        type: integer
                                      successThreshold:
                                        description: Fraction of requests that must
                                          succeed as a percentage string
                                        type: string
                                      threshold:
                                        description: Threshold as a string
                                        type: string
                                      timeoutSeconds:
                                        description: Timeout per request in seconds
                                        type: integer
                                      type:
                                        enum:
                                        - http
                                        - podMetrics
                                        type: string
                                      url:
                                        description: URL to probe (for type=http)
                                        type: string
                                      windowSeconds:
                                        description: Time window in seconds to evaluate
                                          the metric
                                        type: integer
                                    required:
                                    - type
                                    type: object
                                  type: array
                                failedThreshold:
                                  format: int32
                                  type: integer
                                interval:
                                  type: string
                                successThreshold:
                                  format: int32
                                  type: integer
                              type: object
                            canaryService:
                              type: string
                            duration:
                              type: string
                            mirrorPercent:
                              format: int32
                              type: integer
                            stableService:
                              type: string
                          required:
                          - mirrorPercent
                          type: object
                        rolling:
                          description: RollingStrategy performs a standard rolling
                            update.
                          properties:
                            maxSurge:
                              anyOf:
                              - type: integer
                              - type: string
                              x-kubernetes-int-or-string: true
                            maxUnavailable:
                              anyOf:
                              - type: integer
                              - type: string
                              x-kubernetes-int-or-string: true
                          type: object
                        type:
                          enum:
                          - Rolling
                          - Canary
                          - BlueGreen
                          - ABTest
                          - Mirror
                          type: string
                      required:
                      - type
                      type: object
                    strategy:
                      description: Delivery strategy for this stage (overrides spec.strategy
                        if set)
                      type: string
                  required:
                  - name
                  - ring
                  type: object
                type: array
              strategy:
                default: Rolling
                description: |-
                  Strategy is the default delivery strategy for all stages.
                  Can be overridden per-stage.
                enum:
                - Rolling
                - Canary
                - BlueGreen
                type: string
              syncOptions:
                description: SyncOptions fine-tunes how manifests are applied and
                  pruned.
                properties:
                  applyOutOfSyncOnly:
                    description: |-
                      ApplyOutOfSyncOnly skips applying resources whose live state already
                      matches the desired manifest.
                    type: boolean
                  force:
                    description: Force enables force-conflicts for server-side apply.
                    type: boolean
                  prunePropagationPolicy:
                    description: |-
                      PrunePropagationPolicy selects the deletion propagation policy used when
                      pruning managed resources.
                    enum:
                    - Foreground
                    - Background
                    - Orphan
                    type: string
                  replace:
                    description: Replace uses Update instead of server-side apply.
                    type: boolean
                type: object
              syncPolicy:
                default: Auto
                description: SyncPolicy controls whether changes are applied automatically.
                enum:
                - Auto
                - Manual
                type: string
              syncWindows:
                description: SyncWindows restrict when automatic sync may run.
                items:
                  description: SyncWindow defines a cron-based time window that controls
                    automatic sync.
                  properties:
                    duration:
                      description: |-
                        Duration is how long the window stays active after each scheduled start.
                        Parsed with time.ParseDuration, e.g. "8h".
                      type: string
                    kind:
                      description: Kind is whether this window allows or blocks sync.
                      enum:
                      - Allow
                      - Block
                      type: string
                    schedule:
                      description: |-
                        Schedule is a standard 5-field cron expression:
                          MIN HOUR DOM MONTH DOW
                        Example: "0 9 * * MON-FRI" for 09:00 on weekdays.
                      type: string
                    stages:
                      description: Stages limits the window to the named stages. Empty
                        means all stages.
                      items:
                        type: string
                      type: array
                    timezone:
                      description: |-
                        Timezone is an IANA timezone name (e.g. "America/New_York"). Defaults to
                        UTC when empty.
                      type: string
                  required:
                  - duration
                  - kind
                  - schedule
                  type: object
                type: array
            required:
            - source
            - stages
            type: object
          status:
            description: |-
              ApplicationStatus represents the status of an application.
              ApplicationStatus represents the status of an application.
            properties:
              analysisResults:
                description: AnalysisResults aggregate the latest background analysis
                  state.
                items:
                  description: AnalysisResult aggregates the latest state of a single
                    AnalysisRun for the UI/API.
                  properties:
                    checkedAt:
                      format: date-time
                      type: string
                    message:
                      type: string
                    name:
                      type: string
                    passed:
                      type: boolean
                    phase:
                      description: AnalysisRunPhase represents the phase of an analysis
                        run.
                      type: string
                  required:
                  - name
                  - passed
                  - phase
                  type: object
                type: array
              canaryStepIndex:
                description: Index into the canary steps array
                type: integer
              canaryWeight:
                description: Canary weight if currently canarying
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentStage:
                description: Current stage being promoted/verified
                type: string
              gates:
                description: Approval gate status
                items:
                  description: GateStatus represents the current status of an approval
                    gate.
                  properties:
                    approvedBy:
                      type: string
                    message:
                      type: string
                    name:
                      type: string
                    stage:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - name
                  - stage
                  - status
                  type: object
                type: array
              health:
                description: Overall health status computed from health checks
                type: string
              healthChecks:
                description: Results of health check evaluations
                items:
                  description: HealthCheckResult contains the result of a single health
                    check evaluation.
                  properties:
                    checkedAt:
                      format: date-time
                      type: string
                    httpBody:
                      type: string
                    httpStatusCode:
                      description: HTTP probe results
                      type: integer
                    message:
                      type: string
                    name:
                      type: string
                    status:
                      description: HealthStatus represents the health status of an
                        application or resource.
                      type: string
                  required:
                  - name
                  - status
                  type: object
                type: array
              lastSelfHealTime:
                description: LastSelfHealTime records the last time a self-heal action
                  was taken.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation of
                  the spec.
                format: int64
                type: integer
              outOfSync:
                description: OutOfSync resources count
                type: integer
              phase:
                description: |-
                  ApplicationPhase represents the phase of an application.
                  ApplicationPhase represents the phase of an application.
                enum:
                - Pending
                - Building
                - Promoting
                - Canarying
                - Verifying
                - Healthy
                - Degraded
                - Failed
                - RolledBack
                type: string
              pipelineRef:
                type: string
              prunedResources:
                description: Pruned resources count
                type: integer
              releaseRef:
                type: string
              resourceHealth:
                description: ResourceHealth tracks the health status of each deployed
                  resource
                items:
                  description: ResourceHealth tracks the health status of a deployed
                    Kubernetes resource.
                  properties:
                    health:
                      enum:
                      - Healthy
                      - Degraded
                      - Progressing
                      - Unknown
                      - Missing
                      type: string
                    kind:
                      type: string
                    message:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - health
                  - kind
                  - name
                  type: object
                type: array
              resources:
                description: ResourceSync tracks the diff status of each managed resource
                items:
                  description: ResourceSync tracks the sync status of a managed Kubernetes
                    resource.
                  properties:
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    status:
                      enum:
                      - Synced
                      - OutOfSync
                      - Missing
                      - Pruned
                      type: string
                  required:
                  - kind
                  - name
                  - status
                  type: object
                type: array
              revision:
                description: Last deployed revision (git commit hash or chart version)
                type: string
              sourceHash:
                description: Owned resource references
                type: string
              sourceRevision:
                type: string
              stageRefs:
                items:
                  type: string
                type: array
              stages:
                description: Per-stage status
                items:
                  description: |-
                    ApplicationStageStatus represents the status of an application stage.
                    ApplicationStageStatus represents the status of an application stage.
                  properties:
                    name:
                      type: string
                    phase:
                      type: string
                    release:
                      type: string
                    revision:
                      type: string
                    ring:
                      type: integer
                    updatedAt:
                      format: date-time
                      type: string
                  required:
                  - name
                  - ring
                  type: object
                type: array
              synced:
                description: Whether the source has been synced
                type: boolean
              templateRef:
                type: string
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
