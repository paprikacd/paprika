---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: pipelines.pipelines.paprika.io
spec:
  group: pipelines.paprika.io
  names:
    kind: Pipeline
    listKind: PipelineList
    plural: pipelines
    singular: pipeline
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Pipeline represents a CI/CD pipeline.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PipelineSpec defines the specification for a pipeline.
            properties:
              artifacts:
                items:
                  description: PipelineOutput defines an output artifact of a pipeline.
                  properties:
                    name:
                      type: string
                    path:
                      type: string
                    step:
                      type: string
                  required:
                  - name
                  - path
                  type: object
                type: array
              maxParallel:
                type: integer
              sources:
                items:
                  description: Source defines a source for the pipeline.
                  properties:
                    secretRef:
                      type: string
                    type:
                      enum:
                      - git
                      type: string
                    url:
                      type: string
                  required:
                  - type
                  type: object
                type: array
              steps:
                items:
                  description: PipelineStep defines a step in a pipeline.
                  properties:
                    depends:
                      items:
                        type: string
                      type: array
                    image:
                      type: string
                    name:
                      type: string
                    outputs:
                      items:
                        description: PipelineOutput defines an output artifact of
                          a pipeline.
                        properties:
                          name:
                            type: string
                          path:
                            type: string
                          step:
                            type: string
                        required:
                        - name
                        - path
                        type: object
                      type: array
                    retry:
                      type: integer
                    script:
                      type: string
                    timeout:
                      type: integer
                  required:
                  - image
                  - name
                  - script
                  type: object
                type: array
            required:
            - steps
            type: object
          status:
            description: PipelineStatus represents the status of a pipeline.
            properties:
              artifactRefs:
                items:
                  description: PipelineArtifactRef records an artifact produced by
                    a pipeline run.
                  properties:
                    createdAt:
                      format: int64
                      type: integer
                    digest:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    phase:
                      description: PipelineArtifactPhase represents the verification
                        phase of a pipeline artifact.
                      enum:
                      - Pending
                      - Ready
                      - Failed
                      type: string
                    producingStep:
                      type: string
                    reference:
                      type: string
                    resolvedReference:
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
              lastExecutionId:
                type: string
              lastExecutionTime:
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation of
                  the spec.
                format: int64
                type: integer
              phase:
                description: |-
                  PipelinePhase represents the phase of a pipeline.
                  PipelinePhase represents the phase of a pipeline.
                enum:
                - Running
                - Succeeded
                - Failed
                - Cancelled
                type: string
              stepStatuses:
                items:
                  description: StepStatus represents the status of a pipeline step.
                  properties:
                    completedAt:
                      format: date-time
                      type: string
                    logRef:
                      type: string
                    name:
                      type: string
                    phase:
                      description: StepPhase represents the phase of a pipeline step.
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      - Skipped
                      - Cancelled
                      type: string
                    startedAt:
                      format: date-time
                      type: string
                  required:
                  - name
                  - phase
                  type: object
                type: array
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
