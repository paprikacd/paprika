apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml

images:
- name: controller
  newName: controller
  newTag: latest

commonLabels:
  app.kubernetes.io/name: paprika
