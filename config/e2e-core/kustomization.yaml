apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: paprika-system
namePrefix: paprika-

resources:
- ../crd
- ../rbac
- ../manager

patches:
- path: manager_core_patch.yaml
  target:
    kind: Deployment
    name: controller-manager
